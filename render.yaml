services:
  - type: web
    name: dusuncms
    runtime: docker
    plan: free

    dockerfilePath: ./Dockerfile

    healthCheckPath: /

    envVars:
      - key: APP_NAME
        value: DusunCMS

      - key: APP_ENV
        value: production

      - key: APP_DEBUG
        value: "false"

      - key: LOG_CHANNEL
        value: stack

      - key: LOG_LEVEL
        value: error

      - key: FILESYSTEM_DISK
        value: public

      - key: CACHE_STORE
        value: file

      - key: SESSION_DRIVER
        value: file

      - key: QUEUE_CONNECTION
        value: sync